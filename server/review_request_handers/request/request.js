const mongoose = require("mongoose");

// import models
const Request = require("../../models/request");

module.exports = {
  createRequest: (data) => {
    // unfinished code, continue working from here!
  },
};
